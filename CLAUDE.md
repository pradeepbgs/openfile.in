# Project instructions

## Git

- Before pushing any commit that changes code (not docs-only, not just this file), bump the version in the root `package.json` first, as its own `chore: bump version to x.y.z` commit, then push. Don't push code changes with a stale version.
