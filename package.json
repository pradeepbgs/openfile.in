{
  "name": "openfile",
  "license": "AGPL-3.0",
  "version": "0.0.17",
  "private": true,
  "scripts": {
    "dev": "bun run --filter=backend dev & bun run --filter=frontend dev",
    "start": "bun run --filter=backend start & bun run --filter=frontend start",
    "build": "bun run --filter=frontend build",
    "mobile": "bun --cwd apps/mobile run start"
  },
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "dependencies": {
    "@types/node": "^24.9.1"
  }
}